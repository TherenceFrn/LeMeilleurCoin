{% extends "base.html.twig" %}

{% block title %} Home {% endblock %}


{% block body %}
    <table class="table">
        <thead>
        <tr>
            <th scope="col">Id</th>
            <th scope="col">Nom</th>
            <th scope="col">Description</th>
            <th scope="col">City</th>
            <th scope="col">Zip</th>
            <th scope="col">Price</th>
            <th scope="col">Date_Created</th>
            <th scope="col">Author_id</th>
            <th scope="col">Voir</th>
            <th scope="col">Editer</th>
        </tr>
        </thead>
        {% if mesannonces is defined %}

            <tbody>
            {% for annonce in mesannonces %}
                <tr>
                    <th scope="row">{{ annonce.id }}</th>
                    <th>{{ annonce.Title }}</th>
                    <th>{{ annonce.description }}</th>
                    <th>{{ annonce.city }}</th>
                    <th>{{ annonce.zip }}</th>
                    <th>{{ annonce.price }} €</th>
                    <th>{{ annonce.DateCreated.date }}</th>
                    <th>{{ annonce.AuthorId }}</th>


                    <th><a class="btn btn-success" href="{{ path('annonce_index', {'id': annonce.id}) }}">Voir</a></th>
                    <th><a class="btn btn-secondary" href="{{ path('annonce_index', {'id': annonce.id}) }}">Editer</a></th>
                </tr>
            {% endfor %}
            </tbody>
        {% endif %}
    </table>
{% endblock %}